const config = {
    tabWidth: 4,
    singleQuote: true,
    trailingComma: 'none',
    overrides: [
        {
            files: ['*.html', '*.json'],
            options: {
                tabWidth: 2
            }
        }
    ]
};

export default config;
